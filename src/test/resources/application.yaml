spring:
  application:
    name: document_classification_system_springboot-test
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://119.45.164.129:3306/dcs?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&autoReconnect=true&failOverReadOnly=false&maxReconnects=10
    username: root
    password: newpassword
  sql:
    init:
      mode: never  # 改为never，不自动执行schema-test.sql

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: cn.masu.dcs.**.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: assign_id
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

jwt:
  secret: test-secret-test-secret-test-secret-test
  expiration: 86400000

minio:
  endpoint: http://localhost:9000
  access-key: minio
  secret-key: minio123
  bucket-name: document-files
  url-prefix: http://localhost:9000
