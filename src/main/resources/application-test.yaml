spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dcs_test?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password:
  data:
    redis:
      host: localhost
      port: 6379
      password: ""
      database: 2
      timeout: 3000ms

server:
  port: 8081

logging:
  level:
    cn.masu.dcs: info
    com.baomidou.mybatisplus: warn

